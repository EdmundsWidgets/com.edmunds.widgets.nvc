define(['example'], function(example) {

    describe('test for example', function() {

        it('should return bar', function() {
            expect(example.foo()).toBe('bar');
        });

    });

});
